
&НаКлиенте
Процедура ЭксклюзивAPI_ПриЗакрытииПосле(ЗавершениеРаботы)
	
	Если НЕ ЗначениеЗаполнено(Объект.Ссылка) Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭксклюзивAPI_ВыгрузкаВПФ.ОбъектВыгружен(Объект.Ссылка) Тогда
		ПоказатьПредупреждение(,"Документ не выгружен в базу Печатные формы");
	КонецЕсли;
	
КонецПроцедуры
